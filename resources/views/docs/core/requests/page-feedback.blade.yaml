store:
  type: object
  required:
    - url
    - feedback
  properties:
    url:
      type: string
      example: {{ url('/path/to/page') }}
    feedback:
      type: string
      example: This does not work on my browser

update:
  allOf:
    - $ref: "#/store"

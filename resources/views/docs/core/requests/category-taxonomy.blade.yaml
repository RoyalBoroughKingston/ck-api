store:
  type: object
  required:
    - parent_id
    - name
    - order
  properties:
    parent_id:
      type: string
      format: uuid
      nullable: true
    name:
      type: string
    order:
      type: integer

update:
  allOf:
    - $ref: "#/store"

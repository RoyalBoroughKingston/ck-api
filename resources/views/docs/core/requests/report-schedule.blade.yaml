store:
  type: object
  required:
  - report_type
  - repeat_type
  properties:
    report_type:
      type: string
      enum:
      - Commissioners Report
    repeat_type:
      type: string
      enum:
      - weekly
      - monthly

update:
  allOf:
  - $ref: "#/store"

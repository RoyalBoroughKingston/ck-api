store:
  type: object
  required:
    - name
    - order
    - category_taxonomies
  properties:
    name:
      type: string
    order:
      type: integer
    category_taxonomies:
      type: array
      items:
        type: string
        format: uuid

update:
  allOf:
    - $ref: "#/store"

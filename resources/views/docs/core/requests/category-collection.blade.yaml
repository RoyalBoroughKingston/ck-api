store:
  type: object
  required:
    - name
    - intro
    - icon
    - order
    - category_taxonomies
  properties:
    name:
      type: string
    intro:
      type: string
    icon:
      type: string
    order:
      type: integer
    category_taxonomies:
      type: array
      items:
        type: string
        format: uuid

update:
  allOf:
    - $ref: "#/store"

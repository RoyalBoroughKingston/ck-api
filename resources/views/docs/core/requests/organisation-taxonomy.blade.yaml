store:
  type: object
  required:
  - name
  - order
  properties:
    name:
      type: string
      example: Ayup Digital
    order:
      type: integer
      example: 1

update:
  allOf:
  - $ref: "#/store"

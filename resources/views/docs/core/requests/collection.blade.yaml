store:
  type: object
  required:
    - name
    - order
    - taxonomy_categories
  properties:
    name:
      type: string
    order:
      type: integer
    taxonomy_categories:
      type: array
      items:
        type: string
        format: uuid

update:
  allOf:
    - $ref: "#/store"

root:
  get:
    tags: [Search Engine]
    summary: List all the stop words
    description: |
      **Permission:** `Global Admin`
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/resources/stop-words.yaml

  put:
    tags: [Search Engine]
    summary: Update the list of stop words
    description: |
      **Permission**: `Global Admin`
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: /docs/core/requests/stop-words.yaml#/update
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/resources/stop-words.yaml

# /services
root:
  get:
    tags: [Services]
    summary: List all the services
    description: |
      **Permission:** `Open`
    security: []
    responses:
      200:
        content:
          application/json:
            schema:
              allOf:
                - $ref: /docs/core/responses/resource-collection.yaml
                - type: object
                  properties:
                    data:
                      type: array
                      items:
                        $ref: /docs/core/resources/service.yaml

# /services/{service}
nested:
  get:
    tags: [Services]
    summary: Get a specific service
    description: |
      **Permission:** `Open`
    security: []
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/resources/service.yaml

  put:
    tags: [Services]
    summary: Update a specific service
    description: |
      **Permission:** `Service Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: /docs/core/requests/service.yaml#/update
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/resources/service.yaml

  delete:
    tags: [Services]
    summary: Delete a specific service
    description: |
      **Permission:** `Super Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/responses/resource-deleted.yaml

# /services/{service}/logo
logo:
  get:
    tags: [Services]
    summary: Get a specific service's logo
    description: |
      **Permission:** `Open`
    security: []
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          image/png:

  post:
    tags: [Services]
    summary: Upload a specific service's logo
    description: |
      **Permission:** `Service Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        image/png:
          schema:
            type: string
            format: binary
    responses:
      200:
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: The service's logo has been successfully uploaded

  delete:
    tags: [Services]
    summary: Delete a specific service's logo
    description: |
      **Permission:** `Super Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/responses/resource-deleted.yaml

# /services/{service}/seo-image
seo-image:
  get:
    tags: [Services]
    summary: Get a specific service's SEO image
    description: |
      **Permission:** `Service Admin`
    security: []
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          image/png:

  post:
    tags: [Services]
    summary: Upload a specific service's SEO image
    description: |
      **Permission:** `Service Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        image/png:
          schema:
            type: string
            format: binary
    responses:
      200:
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  example: The service's SEO image has been successfully uploaded

  delete:
    tags: [Services]
    summary: Delete a specific service's SEO image
    description: |
      **Permission:** `Service Admin`
    parameters:
      - name: service
        in: path
        description: The ID of the service
        required: true
        schema:
          type: string
          format: uuid
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/responses/resource-deleted.yaml

# /organisations/{organisation}/services
organisations:
  post:
    tags: [Services]
    summary: Create a service for an organisation
    description: |
      **Permission:** `Organisation Admin`
    parameters:
      - name: organisation
        in: path
        description: The ID of the organisation
        required: true
        schema:
          type: string
          format: uuid
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: /docs/core/requests/service.yaml#/store
    responses:
      200:
        content:
          application/json:
            schema:
              $ref: /docs/core/resources/service.yaml
